# Keep the install layout compatible with Astro/Vercel's server-function bundler
# on Windows, where creating pnpm-isolated dependency symlinks requires elevated
# filesystem privileges or Developer Mode.
node-linker=hoisted
